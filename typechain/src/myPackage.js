// @ts-check
/**
 * Initialize the project
 * method {type} 변수명
 * @param {object} config 
 * @param {boolean} config.debug
 * @param {string} config.url
 * @returns boolean
 * 
 */
// 프로젝트 초기화 해줌/**치면바로 자동완성 */ 기본형에서 바꿔주기
export function init(config) {
    return true;
}

/**
 * Exits the program
 * @param {number} code 
 * @returns number
 */
export function exit(code) {
    return code + 1; 
}